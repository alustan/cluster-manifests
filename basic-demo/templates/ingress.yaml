apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backstage-demo
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: backstage-demo
              port:
                name: http